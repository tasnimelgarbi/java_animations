# Space Adventure Animation

## Overview

This animation simulates a space environment with multiple parallax star layers, planets, and a rocket that the user can control.

## Features

- Rocket movement via keyboard
- Moving background with layered stars
- Collision detection with planets

## How to Run

```bash
javac -d build $(find . -name "*.java")
java -cp build spaceadventure.SpaceAdventure
